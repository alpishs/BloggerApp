<nav class="navbar navbar-light bg-dark">
  <div class="container" style="display: block;">
    <%= image_tag "blogger.png",width: "50px", height: "50px", class: "pull-left" %>
    <div class="navbar-header pull-right">
    <% if user_signed_in? %>
      <span class="mb-4 h2"><%= link_to('Logout', destroy_user_session_path) %></span>
    <% else %>
      <span class="mb-4 h2"><%= link_to('Login', new_user_session_path) %></span>
    <% end %>
    </div>
  </div>
</nav>