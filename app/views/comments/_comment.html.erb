<li>
  <%= comment.body %> -
  <small>Submitted <%= time_ago_in_words(comment.created_at) %> ago</small>

  <%= form_for [comment, Comment.new] do |f| %>
    <%= f.text_area :body, placeholder: "Add a Reply" %><br/>
    <%# unless comment.comments.length > 2 %>
    <%= f.submit "Reply"  %>
    <%# end %>
  <% end %>

  <ul>
    <%= render comment.comments %>
  </ul>

</li>